import {CwNotFoundComponent} from './CwNotFound.component';

describe('CwNotFoundComponent', () => {
  let cwNotFound: CwNotFoundComponent;

  beforeEach(() => {
    cwNotFound = new CwNotFoundComponent();
  });

  it('should be defined', () => {
    expect(cwNotFound).toBeDefined();
  });
});
